import 'package:meta/meta.dart';

/// Constants used in the package
abstract class Constants {
  /// The max number of parameters for a command
  @internal
  static const int maxCommandParameters = 3;
}
